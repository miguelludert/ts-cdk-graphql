

import {} from './create-props';
import {} from './create-resources';

export const createDynamoResources = (scope, api, cfSchema) => {
    //const dynamoStacks = filterDataSourcesOfType(cfSchema, );
    


    // 
    

	// separete out data sources, tables, resolvers
	return {
		stacks: {}, // debatable
		datasources: {},
		tables: {},
		resolvers: {},
	};
};